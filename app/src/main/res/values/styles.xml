<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Theme.Apollo" parent="Theme.AppCompat.NoActionBar">
        <!-- Set AppCompat’s color theming attrs -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/darkPrimary</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowBackground">@color/primary</item>
        <item name="android:divider">@color/divider</item>
        <item name="colorButtonNormal">@color/accent</item>

        <!--Api >= 21 Only-->
        <item name="android:colorButtonNormal" tools:targetApi="21">@color/accent</item>
        <item name="android:navigationBarColor" tools:targetApi="21">@color/darkPrimary</item>

        <!-- Other attributes -->
    </style>

</resources>
